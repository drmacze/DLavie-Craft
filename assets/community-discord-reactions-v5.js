(() => {
  'use strict';

  const SB_URL='https://ydaeukhqwishlrjyfktk.supabase.co';
  const SESSION_KEY='sb-ydaeukhqwishlrjyfktk-auth-token';
  const PAGE='.community-page.community-v2';
  const ROUTE=/#\/community(?:$|[/?])/;
  const EMOJI=[
    ['Cepat',['👍','❤️','😂','🔥','🎉','💎','🙏','👀','✨','🤝','😎','😭']],
    ['Wajah',['😀','😄','😁','😆','😅','🤣','😊','🙂','😉','😍','🥰','😘','😋','😜','🤔','🫡','😮','😱','🥹','😢','😭','😤','😡','🤯']],
    ['Gestur',['👍','👎','👏','🙌','🤝','🙏','💪','👌','✌️','🤞','🫶','👀','💯']],
    ['Craft',['⛏️','🪓','⚔️','🛡️','🏹','🧱','🪵','💎','🔥','✨','⚡','💡']],
    ['Perayaan',['🎉','🎊','🥳','🏆','⭐','🌟','🚀','✅','❌','💚','💜','🧡']]
  ];

  let keyPromise=null;
  let state={forums:[],posts:[],comments:[],reactions:[],profiles:[]};
  let observer=null,pageRef=null,raf=0,timer=0,busy=false;

  function session(){try{const s=JSON.parse(localStorage.getItem(SESSION_KEY)||'null');return s?.access_token?s:null;}catch{return null;}}
  function uid(){const s=session();if(s?.user?.id)return s.user.id;try{const p=s?.access_token?.split('.')?.[1];if(!p)return null;return JSON.parse(atob(p.replace(/-/g,'+').replace(/_/g,'/').padEnd(Math.ceil(p.length/4)*4,'=')))?.sub||null;}catch{return null;}}
  async function publicKey(){if(!keyPromise)keyPromise=fetch('/DLavie-Craft/assets/community-chat-v3.js?v=20260904t',{cache:'force-cache'}).then(r=>r.text()).then(t=>t.match(/const SUPABASE_KEY = '([^']+)'/)?.[1]||'');const k=await keyPromise;if(!k)throw new Error('Konfigurasi komunitas belum siap.');return k;}
  async function api(path,options={}){const key=await publicKey(),s=session(),write=!!options.write;const headers={apikey:key,Authorization:`Bearer ${s?.access_token||key}`,'Accept-Profile':'api',Accept:'application/json',...(options.headers||{})};if(write){headers['Content-Type']='application/json';headers['Content-Profile']='api';headers.Prefer='return=minimal';}const res=await fetch(`${SB_URL}/rest/v1/${path}`,{...options,headers});const text=await res.text();let body=null;try{body=text?JSON.parse(text):null;}catch{body=text;}if(!res.ok)throw new Error(body?.message||body?.details||body?.hint||body||`Server ${res.status}`);return body;}
  async function load(){const [forums,posts,comments,reactions,profiles]=await Promise.all([api('dlavie_craft_community_forums?select=id,name,slug,forum_type&is_active=eq.true&order=sort_order.asc'),api('dlavie_craft_community_posts?select=id,author_id,forum_id,body,created_at,updated_at&status=eq.published&order=created_at.desc'),api('dlavie_craft_community_comments?select=id,post_id,author_id,parent_comment_id,body,created_at,updated_at&status=eq.published&order=created_at.asc'),api('dlavie_craft_community_reactions?select=id,post_id,comment_id,author_id,emoji,created_at&order=created_at.asc'),api('dlavie_craft_community_leaderboard?select=user_id,display_name,level')]);state={forums:forums||[],posts:posts||[],comments:comments||[],reactions:reactions||[],profiles:profiles||[]};return state;}
  function nameOf(id){return state.profiles.find(p=>p.user_id===id)?.display_name||'Crafter';}
  function activeForum(page){const n=page.querySelector('.active-forum-head h2')?.textContent?.trim()||page.querySelector('.forum-sidebar nav button.active strong')?.textContent?.trim();return state.forums.find(f=>f.name===n)||null;}
  function button(text,cls=''){const b=document.createElement('button');b.type='button';b.textContent=text;b.className=cls;b.setAttribute('data-dl-no-icon','true');return b;}
  function toast(text){document.querySelector('.dl-v4-toast')?.remove();const n=document.createElement('div');n.className='dl-v4-toast';n.textContent=text;document.body.append(n);requestAnimationFrame(()=>n.classList.add('show'));setTimeout(()=>{n.classList.remove('show');setTimeout(()=>n.remove(),180);},2200);}
  function login(){(document.getElementById('dl-shell-account-entry')||document.getElementById('dl-account-entry'))?.click();}

  function grouped(kind,id){const key=kind==='post'?'post_id':'comment_id',me=uid(),map=new Map();state.reactions.filter(r=>r[key]===id).forEach(r=>{const g=map.get(r.emoji)||{emoji:r.emoji,count:0,mine:false,names:[]};g.count+=1;if(r.author_id===me)g.mine=true;g.names.push(nameOf(r.author_id));map.set(r.emoji,g);});return [...map.values()];}
  async function toggle(kind,id,emoji){if(!session())return login();if(busy)return;busy=true;const me=uid(),key=kind==='post'?'post_id':'comment_id',existing=state.reactions.find(r=>r[key]===id&&r.author_id===me&&r.emoji===emoji);try{if(existing){await api(`dlavie_craft_community_reactions?${key}=eq.${encodeURIComponent(id)}&author_id=eq.${encodeURIComponent(me)}&emoji=eq.${encodeURIComponent(emoji)}`,{method:'DELETE',write:true});state.reactions=state.reactions.filter(r=>r!==existing);}else{const payload={post_id:kind==='post'?id:null,comment_id:kind==='comment'?id:null,author_id:me,emoji};await api('dlavie_craft_community_reactions',{method:'POST',write:true,body:JSON.stringify(payload)});state.reactions.push({id:`local-${Date.now()}`,...payload,created_at:new Date().toISOString()});}render(document.querySelector(PAGE));refresh(180);}catch(e){toast(`Reaction gagal: ${e.message}`);}finally{busy=false;}}

  function reactionRow(kind,id){const row=document.createElement('div');row.className='dl-v4-reactions';grouped(kind,id).forEach(g=>{const b=button('',`dl-v4-react-chip${g.mine?' mine':''}`),emoji=document.createElement('span'),count=document.createElement('b');emoji.textContent=g.emoji;count.textContent=String(g.count);b.append(emoji,count);b.title=g.names.slice(0,8).join(', ');b.setAttribute('aria-label',`${g.emoji}, ${g.count} reaction${g.mine?', kamu ikut bereaksi':''}`);b.onclick=()=>toggle(kind,id,g.emoji);row.append(b);});const add=button('＋','dl-v4-react-add');add.setAttribute('aria-label','Tambah reaction');add.onclick=e=>openPicker(kind,id,e.currentTarget);row.append(add);return row;}
  function openPicker(kind,id,anchor){if(!session())return login();document.querySelector('.dl-v4-picker')?.remove();const picker=document.createElement('div');picker.className='dl-v4-picker';const head=document.createElement('header'),title=document.createElement('strong'),search=document.createElement('input'),close=button('×');title.textContent='Reaction';search.type='search';search.placeholder='Cari emoji…';close.setAttribute('aria-label','Tutup');head.append(title,search,close);const list=document.createElement('div');list.className='dl-v4-emoji-list';picker.append(head,list);const draw=query=>{list.replaceChildren();EMOJI.forEach(([label,items])=>{if(query&&!label.toLowerCase().includes(query.toLowerCase()))return;const section=document.createElement('section'),small=document.createElement('small'),grid=document.createElement('div');small.textContent=label;items.forEach(emoji=>{const b=button(emoji,'dl-v4-emoji');b.onclick=()=>{picker.remove();toggle(kind,id,emoji);};grid.append(b);});section.append(small,grid);list.append(section);});};draw('');search.oninput=()=>draw(search.value.trim());close.onclick=()=>picker.remove();document.body.append(picker);if(matchMedia('(min-width:720px)').matches){const r=anchor.getBoundingClientRect();picker.style.left=`${Math.max(12,Math.min(innerWidth-350,r.left-100))}px`;picker.style.top=`${Math.min(innerHeight-400,r.bottom+8)}px`;}}

  function render(page){if(!page)return;page.classList.add('dl-community-discord-v4');page.querySelectorAll('.dl-v4-reactions').forEach(n=>n.remove());page.querySelectorAll('.reaction-area').forEach(n=>n.classList.add('dl-v4-hide-native-reaction'));const forum=activeForum(page);if(!forum)return;const posts=state.posts.filter(p=>p.forum_id===forum.id),entries=[...page.querySelectorAll('.typed-feed > .community-entry')];entries.forEach((entry,i)=>{const post=posts[i];if(!post)return;entry.dataset.dlPostId=post.id;entry.classList.add('dl-v4-message');const actions=entry.querySelector(':scope > .entry-actions')||entry.querySelector('.entry-actions');actions?.prepend(reactionRow('post',post.id));const comments=state.comments.filter(c=>c.post_id===post.id),nodes=[...entry.querySelectorAll('.post-thread .community-comment')];nodes.forEach((node,j)=>{const c=comments[j];if(!c)return;node.dataset.dlCommentId=c.id;const content=node.children?.[1]||node;let host=content.querySelector(':scope > .dl-v4-comment-actions');if(!host){host=document.createElement('div');host.className='dl-v4-comment-actions';content.append(host);}host.prepend(reactionRow('comment',c.id));});});const latest=page.querySelector('[data-dl-community-action="latest"]');latest?.setAttribute('data-dl-no-icon','true');}

  function goLatest(page){const entry=page.querySelector('.typed-feed > .community-entry');if(entry){entry.scrollIntoView({behavior:'smooth',block:'center'});entry.classList.add('dl-v4-highlight');setTimeout(()=>entry.classList.remove('dl-v4-highlight'),850);return;}const candidates=[...page.querySelectorAll('.forum-sidebar nav button')].filter(b=>Number((b.querySelector('b')?.textContent||'0').replace(/\D/g,''))>0),target=candidates.find(b=>!b.classList.contains('active'))||candidates[0];if(!target)return toast('Belum ada aktivitas baru.');target.click();setTimeout(()=>page.querySelector('.typed-feed > .community-entry')?.scrollIntoView({behavior:'smooth',block:'center'}),300);}
  function schedule(page=document.querySelector(PAGE)){if(raf)return;raf=requestAnimationFrame(()=>{raf=0;if(page?.isConnected)render(page);});}
  function refresh(delay=260){clearTimeout(timer);timer=setTimeout(async()=>{if(!ROUTE.test(location.hash))return;try{await load();schedule();}catch(e){console.warn('[Discord reactions]',e.message);}},delay);}
  function attach(page){if(pageRef===page){schedule(page);return;}observer?.disconnect();pageRef=page;load().then(()=>schedule(page)).catch(e=>console.warn('[Discord reactions]',e.message));page.addEventListener('click',e=>{if(e.target.closest('.forum-sidebar nav button'))setTimeout(()=>refresh(80),100);});observer=new MutationObserver(records=>{const meaningful=records.some(r=>{const t=r.target?.nodeType===1?r.target:r.target?.parentElement;return !t?.closest?.('.dl-v4-reactions,.dl-v4-picker,.dl-message-menu,.dl-message-more,.dl-reply-context,.dl-thread-head,.dl-community-v3-sheet');});if(meaningful)schedule(page);});observer.observe(page,{childList:true,subtree:true});}
  function route(){if(!ROUTE.test(location.hash)){observer?.disconnect();observer=null;pageRef=null;document.querySelector('.dl-v4-picker')?.remove();return;}let n=0;const wait=()=>{const p=document.querySelector(PAGE);if(p)return attach(p);if(n++<30)setTimeout(wait,80+n*8);};wait();}

  document.addEventListener('click',e=>{if(!ROUTE.test(location.hash))return;const latest=e.target.closest?.('[data-dl-community-action="latest"]');if(latest){e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();const page=document.querySelector(PAGE);if(page)goLatest(page);return;}const picker=document.querySelector('.dl-v4-picker');if(picker&&!e.target.closest?.('.dl-v4-picker,.dl-v4-react-add'))picker.remove();},true);
  window.addEventListener('hashchange',route);window.addEventListener('popstate',route);window.addEventListener('pageshow',route);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',route,{once:true});else route();
  window.__DLAVIE_COMMUNITY_V4__={version:'20260905v',api,load,getState:()=>state,scheduleDOM:schedule,toast,publicKey,session,uid};
})();